# Tutorial + UI Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 쉬움 난이도에서 선택적 인게임 튜토리얼(오버레이 + 6단계 실습) + 말 크기 2배 + UI 개선(소환 아이콘, 기권 확인창)

**Architecture:** TutorialScene이 GameScene 위에 오버레이로 실행. GameScene에 tutorialMode/tutorialLocked 플래그와 이벤트 emit 5곳 추가. PlacementScene 쉬움 자동배치 후 팝업으로 진입 여부 결정.

**Tech Stack:** Phaser 3.87, Vite, vanilla JS

---

## File Map

| 파일 | 변경 |
|------|------|
| `src/scenes/GameScene.js` | 말 크기 144px, 튜토리얼 플래그·이벤트·잠금 |
| `src/scenes/UIScene.js` | 소환 버튼 아이콘화, 기권 확인창 |
| `src/scenes/PlacementScene.js` | 쉬움 모드 튜토리얼 팝업 |
| `src/scenes/TutorialScene.js` | 신규 — 오버레이 튜토리얼 |
| `src/main.js` | TutorialScene 등록 |

---

## Task 1: 말 크기 2배 + 기권 확인창 + 소환 아이콘

**Files:**
- Modify: `src/scenes/GameScene.js` (setDisplaySize 72 → 144)
- Modify: `src/scenes/UIScene.js` (소환 아이콘, 기권 확인)

- [ ] **Step 1: GameScene 말 크기 변경**

`src/scenes/GameScene.js`의 두 곳을 수정:

`_renderPiece()` 안:
```js
const obj = this.add.image(x, y, key)
  .setDisplaySize(144, 144)
  .setDepth(4);
```

`_animateMove()` 안:
```js
const animPiece = this.add.image(fromX, fromY, key)
  .setDisplaySize(144, 144)
  .setDepth(6);
```

- [ ] **Step 2: UIScene 소환 버튼 아이콘 + 마나 숫자만 표시**

`src/scenes/UIScene.js`의 소환 버튼 생성 부분을 수정.

현재:
```js
const label = `${PIECE_LABELS[type]}  (${SUMMON_COSTS[type]} 마나)`;
const txt = this.add.text(PANEL_X + 90, y, label, {
  fontSize: '16px', color: '#444466',
}).setOrigin(0.5).setAlpha(0.5);
```

변경 — 버튼 안에 piece 아이콘 + 마나 숫자:
```js
// 말 아이콘 (이미지 키는 GameScene이 이미 캐시에 로드)
const icon = this.add.image(PANEL_X + 20, y, `${type.toLowerCase()}_w`)
  .setDisplaySize(32, 32).setAlpha(0.5);
const txt = this.add.text(PANEL_X + 90, y, `${SUMMON_COSTS[type]}`, {
  fontSize: '18px', color: '#444466',
}).setOrigin(0.5).setAlpha(0.5);
```

또한 `summonButtons[type]`에 `icon`도 저장하고 `_refreshSummonButtons`와 `_highlightActiveSummon`에서 icon alpha/color도 동기화:

```js
this.summonButtons[type] = { btn, txt, icon };
```

`_refreshSummonButtons` 수정:
```js
_refreshSummonButtons(playerMana, hasSummoned, summonCounts) {
  for (const [type, { btn, txt, icon }] of Object.entries(this.summonButtons)) {
    const count = summonCounts?.[type] || 0;
    const cost = (SUMMON_COSTS[type] || 1) + count;
    const enabled = !hasSummoned && playerMana >= cost;
    btn.setData('enabled', enabled);
    btn.setFillStyle(enabled ? COLORS.BUTTON_BG : 0x1a1a2e);
    btn.setAlpha(enabled ? 1 : 0.45);
    txt.setText(String(cost));
    txt.setColor(enabled ? TEXT_COLORS.PRIMARY : '#444466');
    txt.setAlpha(enabled ? 1 : 0.5);
    icon.setAlpha(enabled ? 1 : 0.3);
  }
}
```

`_highlightActiveSummon` 수정:
```js
_highlightActiveSummon(activeType) {
  for (const [type, { btn, txt, icon }] of Object.entries(this.summonButtons)) {
    const isActive = type === activeType;
    btn.setData('active', isActive);
    if (btn.getData('enabled')) {
      btn.setFillStyle(isActive ? 0xffaa00 : COLORS.BUTTON_BG);
      btn.setAlpha(1);
      txt.setColor(isActive ? '#1a1a2e' : TEXT_COLORS.PRIMARY);
      icon.setAlpha(1);
    }
  }
}
```

- [ ] **Step 3: UIScene 기권 버튼 확인창 추가**

기존 surrender 버튼 클릭 핸들러 수정 — 직접 surrender() 호출 대신 확인 팝업 표시:

```js
surrenderBtn.on('pointerdown', () => this._showSurrenderConfirm());
```

`UIScene`에 메서드 추가:
```js
_showSurrenderConfirm() {
  const cx = LAYOUT.GAME_WIDTH / 2;
  const cy = LAYOUT.GAME_HEIGHT / 2;
  const overlay = this.add.rectangle(cx, cy, LAYOUT.GAME_WIDTH, LAYOUT.GAME_HEIGHT, 0x000000, 0.6).setDepth(50);
  const panel = this.add.rectangle(cx, cy, 280, 130, 0x1a1a2e).setDepth(51);
  const border = this.add.rectangle(cx, cy, 284, 134, 0xff4444).setDepth(50);
  const msg = this.add.text(cx, cy - 30, '정말 기권하시겠습니까?', {
    fontSize: '18px', color: '#ffffff',
  }).setOrigin(0.5).setDepth(52);

  const yesBtn = this.add.rectangle(cx - 60, cy + 30, 100, 36, 0xaa2222).setInteractive().setDepth(52);
  this.add.text(cx - 60, cy + 30, '기권', { fontSize: '16px', color: '#ffffff' }).setOrigin(0.5).setDepth(53);
  const noBtn = this.add.rectangle(cx + 60, cy + 30, 100, 36, 0x333355).setInteractive().setDepth(52);
  this.add.text(cx + 60, cy + 30, '취소', { fontSize: '16px', color: '#ffffff' }).setOrigin(0.5).setDepth(53);

  const close = () => [overlay, panel, border, msg, yesBtn, noBtn].forEach(o => o.destroy());
  yesBtn.on('pointerdown', () => { close(); this.gameScene.surrender(); });
  noBtn.on('pointerdown', close);
  overlay.setInteractive().on('pointerdown', close);
}
```

UIScene imports에 `LAYOUT` 추가 (아직 없으면):
```js
import { COLORS, LAYOUT, Owner, PieceType, SUMMON_COSTS, PIECE_LABELS, TEXT_COLORS } from '../config.js';
```

- [ ] **Step 4: 빌드 확인**
```bash
cd C:/Development/17_DC && npm run build 2>&1 | tail -5
```
Expected: `✓ built in ...`

- [ ] **Step 5: 커밋**
```bash
git add src/scenes/GameScene.js src/scenes/UIScene.js
git commit -m "feat: double piece size, summon icons, surrender confirmation"
```

---

## Task 2: GameScene 튜토리얼 훅

**Files:**
- Modify: `src/scenes/GameScene.js`

- [ ] **Step 1: init()에 tutorialMode 추가**

```js
init(data) {
  this.difficulty = data.difficulty;
  this.playerPlacements = data.playerPlacements;
  this.tutorialMode = data.tutorialMode || false;
}
```

- [ ] **Step 2: create()에 tutorialLocked + TutorialScene 실행**

`this.scene.launch('UI');` 바로 위에 추가:
```js
this.tutorialLocked = false;
```
`this._startTurn(Owner.PLAYER);` 바로 아래에 추가:
```js
if (this.tutorialMode) this.scene.launch('Tutorial');
```

- [ ] **Step 3: _onCellClick 잠금 + 이벤트 emit**

함수 맨 위 두 번째 줄에 추가:
```js
if (this.tutorialLocked) return;
```

폰/말 선택 직후 (state = SELECTED 설정 이후):
```js
if (this.tutorialMode) this.events.emit('tutorial-piece-selected');
```

이동 완료 콜백 (`this.hasMoved = true;` 직후):
```js
if (this.tutorialMode) this.events.emit('tutorial-piece-moved');
```

소환 완료 직후 (`this.hasSummoned = true;` 직후):
```js
if (this.tutorialMode) this.events.emit('tutorial-summoned');
```

소환 후 auto-endTurn 로직 수정 (tutorialMode면 auto-end 건너뜀):
```js
if (this.hasMoved) {
  if (!this.tutorialMode) { this._endTurn(); return; }
  this._emitPlayerAction();
  return;
}
```

- [ ] **Step 4: startSummonMode 잠금 + emit**

`startSummonMode` 맨 앞에 추가:
```js
if (this.tutorialLocked) return;
```

소환 모드 진입 성공 후 (state = SUMMON_MODE 설정 이후):
```js
if (this.tutorialMode) this.events.emit('tutorial-summon-clicked');
```

- [ ] **Step 5: endTurnManually emit**

```js
endTurnManually() {
  if (this.state === State.WAITING && this.board.currentTurn === Owner.PLAYER) {
    if (this.tutorialMode) this.events.emit('tutorial-turn-ended');
    this._endTurn();
  }
}
```

- [ ] **Step 6: 빌드 확인**
```bash
npm run build 2>&1 | tail -5
```

- [ ] **Step 7: 커밋**
```bash
git add src/scenes/GameScene.js
git commit -m "feat: add tutorial hooks to GameScene"
```

---

## Task 3: TutorialScene (신규)

**Files:**
- Create: `src/scenes/TutorialScene.js`

- [ ] **Step 1: 파일 생성**

```js
// src/scenes/TutorialScene.js
import { LAYOUT, COLORS } from '../config.js';

const BOARD_X = LAYOUT.BOARD_OFFSET_X;
const BOARD_Y = LAYOUT.BOARD_OFFSET_Y;
const BOARD_W = 5 * LAYOUT.CELL_SIZE;
const BOARD_H = 5 * LAYOUT.CELL_SIZE;
const PANEL_X = 510;

const HIGHLIGHT_RECTS = {
  board:    { x: BOARD_X - 4,  y: BOARD_Y - 4,   w: BOARD_W + 8, h: BOARD_H + 8 },
  summonUI: { x: PANEL_X - 4,  y: 205,            w: 198,         h: 260 },
  endBtn:   { x: PANEL_X - 4,  y: 522,            w: 198,         h: 36 },
};

const STEPS = [
  { text: '폰을 클릭해보세요',                       highlight: 'board',    waitEvent: 'tutorial-piece-selected' },
  { text: '이동할 칸을 선택하세요',                   highlight: 'board',    waitEvent: 'tutorial-piece-moved' },
  { text: '마나 +2!\n소환 버튼을 눌러보세요',          highlight: 'summonUI', waitEvent: 'tutorial-summon-clicked' },
  { text: '왕 주변 빈 칸을 클릭해\n소환하세요',        highlight: 'board',    waitEvent: 'tutorial-summoned' },
  { text: '턴 종료 버튼을 눌러\nAI에게 턴을 넘기세요', highlight: 'endBtn',   waitEvent: 'tutorial-turn-ended' },
  { text: '튜토리얼 완료!\n자유롭게 플레이하세요',     highlight: null,       waitEvent: null },
];

export class TutorialScene extends Phaser.Scene {
  constructor() { super({ key: 'Tutorial', active: false }); }

  create() {
    this.gameScene = this.scene.get('Game');
    this.stepIndex = 0;
    this._overlayObjs = [];

    this.gameScene.events.on('tutorial-piece-selected', () => this._tryAdvance('tutorial-piece-selected'), this);
    this.gameScene.events.on('tutorial-piece-moved',    () => this._tryAdvance('tutorial-piece-moved'),    this);
    this.gameScene.events.on('tutorial-summon-clicked', () => this._tryAdvance('tutorial-summon-clicked'), this);
    this.gameScene.events.on('tutorial-summoned',       () => this._tryAdvance('tutorial-summoned'),       this);
    this.gameScene.events.on('tutorial-turn-ended',     () => this._tryAdvance('tutorial-turn-ended'),     this);
    this.gameScene.events.on('check', (inCheck) => {
      if (inCheck && this.stepIndex < STEPS.length - 1) this._showHint('체크! 이동으로 왕의 위협을 피하세요.');
    }, this);

    this._showStep();
  }

  _tryAdvance(event) {
    const step = STEPS[this.stepIndex];
    if (step && step.waitEvent === event) this._nextStep();
  }

  _nextStep() {
    this.stepIndex++;
    this._showStep();
  }

  _clearOverlay() {
    this._overlayObjs.forEach(o => o.destroy());
    this._overlayObjs = [];
  }

  _showStep() {
    this._clearOverlay();
    const step = STEPS[this.stepIndex];
    if (!step) { this._finish(); return; }

    const W = LAYOUT.GAME_WIDTH, H = LAYOUT.GAME_HEIGHT;

    // 반투명 전체 오버레이
    const overlay = this.add.rectangle(W / 2, H / 2, W, H, 0x000000, 0.55).setDepth(20).setInteractive();
    this._overlayObjs.push(overlay);

    // 하이라이트 테두리
    if (step.highlight) {
      const r = HIGHLIGHT_RECTS[step.highlight];
      const border = this.add.graphics().setDepth(21);
      border.lineStyle(3, 0xffaa00, 1);
      border.strokeRect(r.x, r.y, r.w, r.h);
      // 깜빡임
      this.tweens.add({ targets: border, alpha: 0.3, duration: 600, yoyo: true, repeat: -1 });
      this._overlayObjs.push(border);
    }

    // 텍스트 박스 위치 (하이라이트 아래, 없으면 중앙)
    const boxY = step.highlight
      ? Math.min(HIGHLIGHT_RECTS[step.highlight].y + HIGHLIGHT_RECTS[step.highlight].h + 50, H - 80)
      : H / 2;
    const boxX = step.highlight === 'summonUI' || step.highlight === 'endBtn'
      ? PANEL_X + 95
      : W / 2;

    const lines = step.text.split('\n').length;
    const boxH = 40 + lines * 24;

    const panelBorder = this.add.rectangle(boxX, boxY, 284, boxH + 4, 0xffaa00).setDepth(21);
    const panel = this.add.rectangle(boxX, boxY, 280, boxH, 0x1a1a2e).setDepth(22);
    const txt = this.add.text(boxX, boxY, step.text, {
      fontSize: '16px', color: '#ffffff', align: 'center', lineSpacing: 6,
    }).setOrigin(0.5).setDepth(23);
    this._overlayObjs.push(panelBorder, panel, txt);

    // 진행 점
    const dotsY = boxY + boxH / 2 + 18;
    const total = STEPS.length - 1; // 마지막(done) 제외
    const dotSpacing = 14;
    const startX = boxX - (total - 1) * dotSpacing / 2;
    for (let i = 0; i < total; i++) {
      const dot = this.add.circle(startX + i * dotSpacing, dotsY, 4,
        i < this.stepIndex ? 0xffaa00 : (i === this.stepIndex ? 0xffffff : 0x555555)).setDepth(23);
      this._overlayObjs.push(dot);
    }

    // 마지막 단계: "확인" 버튼
    if (!step.waitEvent) {
      const confirmBtn = this.add.rectangle(boxX, boxY + boxH / 2 + 40, 100, 34, 0x2a8a4a).setInteractive().setDepth(23);
      const confirmTxt = this.add.text(boxX, boxY + boxH / 2 + 40, '확인', {
        fontSize: '16px', color: '#ffffff',
      }).setOrigin(0.5).setDepth(24);
      this._overlayObjs.push(confirmBtn, confirmTxt);
      confirmBtn.on('pointerdown', () => this._finish());
    }

    // 튜토리얼 중 잠금 (마지막 단계는 잠금 해제)
    this.gameScene.tutorialLocked = step.waitEvent !== null && step.highlight !== null
      ? false // 잠금은 하지 않고 이벤트만 감지
      : false;
  }

  _showHint(text) {
    const hint = this.add.text(LAYOUT.GAME_WIDTH / 2, 20, text, {
      fontSize: '15px', color: '#ffdd00', backgroundColor: '#1a1a2e',
      padding: { x: 10, y: 5 },
    }).setOrigin(0.5).setDepth(30);
    this.time.delayedCall(3000, () => hint.destroy());
  }

  _finish() {
    this._clearOverlay();
    this.gameScene.tutorialMode = false;
    this.gameScene.tutorialLocked = false;
    this.scene.stop();
  }

  shutdown() {
    if (this.gameScene) {
      ['tutorial-piece-selected','tutorial-piece-moved','tutorial-summon-clicked',
       'tutorial-summoned','tutorial-turn-ended','check'].forEach(ev =>
        this.gameScene.events.off(ev, undefined, this));
    }
  }
}
```

- [ ] **Step 2: 빌드 확인**
```bash
npm run build 2>&1 | tail -5
```

- [ ] **Step 3: 커밋**
```bash
git add src/scenes/TutorialScene.js
git commit -m "feat: add TutorialScene overlay"
```

---

## Task 4: PlacementScene 튜토리얼 팝업 + main.js 등록

**Files:**
- Modify: `src/scenes/PlacementScene.js`
- Modify: `src/main.js`

- [ ] **Step 1: main.js에 TutorialScene 등록**

```js
import { TutorialScene } from './scenes/TutorialScene.js';
// ...
scene: [BootScene, MenuScene, PlacementScene, GameScene, UIScene, ResultScene, TutorialScene],
```

- [ ] **Step 2: PlacementScene _autoStart()를 Easy 전용 팝업으로 수정**

현재:
```js
_autoStart() {
  // ... shuffle ...
  this.time.delayedCall(50, () => {
    this.scene.start('Game', { difficulty: this.difficulty, playerPlacements: placements });
  });
}
```

변경:
```js
_autoStart() {
  const cells = [];
  for (let r = 3; r <= 4; r++)
    for (let c = 0; c < 5; c++)
      if (!(r === KING_ROW && c === KING_COL)) cells.push({ row: r, col: c });
  for (let i = cells.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [cells[i], cells[j]] = [cells[j], cells[i]];
  }
  const placements = cells.slice(0, PAWN_COUNT);

  if (this.difficulty === Difficulty.EASY) {
    this._showTutorialPrompt(placements);
  } else {
    this.time.delayedCall(50, () => {
      this.scene.start('Game', { difficulty: this.difficulty, playerPlacements: placements });
    });
  }
}

_showTutorialPrompt(placements) {
  const cx = LAYOUT.GAME_WIDTH / 2, cy = LAYOUT.GAME_HEIGHT / 2;
  this.add.rectangle(cx, cy, LAYOUT.GAME_WIDTH, LAYOUT.GAME_HEIGHT, COLORS.PANEL_BG);

  this.add.text(cx, cy - 60, '튜토리얼을 볼까요?', {
    fontSize: '26px', color: '#ffffff', fontStyle: 'bold',
  }).setOrigin(0.5);
  this.add.text(cx, cy - 20, '게임 방법을 단계별로 알려드립니다', {
    fontSize: '16px', color: '#aaaaaa',
  }).setOrigin(0.5);

  const yesBtn = this.add.rectangle(cx - 70, cy + 40, 120, 44, 0x2a8a4a).setInteractive();
  this.add.text(cx - 70, cy + 40, '네', { fontSize: '20px', color: '#ffffff' }).setOrigin(0.5);
  const noBtn = this.add.rectangle(cx + 70, cy + 40, 120, 44, 0x444466).setInteractive();
  this.add.text(cx + 70, cy + 40, '아니오', { fontSize: '20px', color: '#ffffff' }).setOrigin(0.5);

  yesBtn.on('pointerdown', () =>
    this.scene.start('Game', { difficulty: this.difficulty, playerPlacements: placements, tutorialMode: true }));
  noBtn.on('pointerdown', () =>
    this.scene.start('Game', { difficulty: this.difficulty, playerPlacements: placements }));
}
```

- [ ] **Step 3: 빌드 확인**
```bash
npm run build 2>&1 | tail -5
```

- [ ] **Step 4: 커밋**
```bash
git add src/scenes/PlacementScene.js src/main.js
git commit -m "feat: add tutorial prompt in PlacementScene, register TutorialScene"
```

---

## Task 5: 빌드 및 배포

- [ ] **Step 1: 전체 dist 빌드**
```bash
cd C:/Development/17_DC && npm run dist 2>&1 | tail -10
```
Expected: `building target=portable file=release\ChessSummon_v0.1.0_portable.exe`

- [ ] **Step 2: 루트에 exe 복사**
```bash
cp release/ChessSummon_v0.1.0_portable.exe ChessSummon_v0.1.0_portable.exe
```

- [ ] **Step 3: 최종 커밋 및 푸시**
```bash
git add ChessSummon_v0.1.0_portable.exe
git commit -m "build: update portable exe with tutorial and UI improvements"
git push
```
